{
  "name": "sistema-socio-club",
  "version": "1.0.0",
  "description": "Sistema de gestión de socios para club deportivo y social",
  "scripts": {
    "dev": "netlify dev",
    "build": "cd frontend && npm run build",
    "install:all": "npm install && cd frontend && npm install"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.5",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "netlify-cli": "^17.37.2"
  }
}
